import {expect, test} from 'vitest';
// import {isBrowser} from '@loaders.gl/core';
// @ts-ignore TS2304: Cannot find name '__VERSION__'.
// const version = typeof __VERSION__ !== 'undefined' ? __VERSION__ : '';
test('loaders has global version', () => {});
