import {LoaderObject} from '../../types';

/**
 *
 * @param loader
 * @param coreVersion
 * @returns `true` if the two versions are compatible
 */
export function validateLoaderVersion(loader: LoaderObject, coreVersion?: string): boolean;
