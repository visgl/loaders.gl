{
  "name": "bundle-with-vite",
  "description": "Minimal example of using loaders.gl with vite.",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    "@loaders.gl/core": "workspace:*",
    "@loaders.gl/gltf": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.2.7"
  }
}
