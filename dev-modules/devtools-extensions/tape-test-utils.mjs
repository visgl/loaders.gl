import test, {
  isBrowser,
  isNode,
  testIf,
  testIfBrowser,
  testIfNode
} from './vitest/vitest-tape.mjs';

export {isBrowser, isNode, test, testIf, testIfBrowser, testIfNode};
export default test;
