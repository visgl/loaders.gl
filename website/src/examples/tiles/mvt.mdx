# Mapbox Vector Tiles

import Demo from 'examples/website/tiles/app';

<div style={{height: '100vh'}}> 
  <Demo format="MVT">
    <div><code><b>MVTSource</b></code> dynamically loads tile files from a hierarchy of pre-tiled tile files, and parses them into GeoJSON or binary format.</div>
    <div>It can load both vector tiles in Mapbox Vector Tiles format as well as image tiles in standard image formats.</div>
    <div>(An <code><b>MVTLoader</b></code> is also available that parses individual tiles).</div>
  </Demo>
</div>
