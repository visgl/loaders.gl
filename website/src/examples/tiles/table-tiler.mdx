# GeoJSON Tiles

import Demo from 'examples/website/tiles/app';

<div style={{height: '100vh'}}> 
  <Demo format="GeoJSON">
    <div>A <code><b>TableTileSource</b></code> dynamically <b>generating</b> tiles from GeoJSON data loaded into the browser.</div>
  </Demo>
</div>
