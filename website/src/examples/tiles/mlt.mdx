# Maplibre Tiles (MLT)

import Demo from './mlt'

<div style={{height: '100vh'}}> 
  <Demo>
    [**`MLTLoader`**](/docs/modules/mlt/api-reference/mlt-loader) loads [MapLibre tile](/docs/modules/mlt/formats/mlt) (`.mlt`) data in the standalone MapLibre example while the shared `MLTSource` TileSource path is still being debugged.
  </Demo>
</div>
