# OME-Zarr Source

import Demo from 'examples/website/omezarr/app';

<div style={{height: 'calc(100vh - var(--ifm-navbar-height))'}}>
  <Demo rootUrl="/examples/bioimaging/ome-zarr/spatialdata.zarr">
    [**`OMEZarrSourceLoader`**](/docs/modules/zarr) browses a consolidated Zarr root, then opens an image group as an OME-Zarr source with multiscale metadata and plane reads.
  </Demo>
</div>
